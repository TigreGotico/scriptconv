"""Quarantined vendored copy — see ``LICENSE.md`` in this directory.

Nothing imports at package import time; callers reach in explicitly via
``scriptconv.phonemizers._vendored.iqra_phonetiser.phonetiser`` (what
:class:`scriptconv.phonemizers.ar.IqraPhonemizer` does), matching the same
opt-in pattern as ``_vendored/mantoq``.
"""
